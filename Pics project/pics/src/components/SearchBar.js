function SearchBar(onSubmit) {
  const handleClick = () => {};
  return (
    <div>
      <input />
      <button onClick={handleClick}>Click me!</button>
    </div>
  );
}

export default SearchBar;
